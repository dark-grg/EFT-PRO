# ============================================================
# PES ARENA — Environment Variables Template
# ============================================================

# Server-Side Secrets (Keep strictly on server)
GEMINI_API_KEY=YOUR_SERVER_SIDE_GEMINI_API_KEY

# Optional: Set custom API base URL for standalone Android deployment
# If empty or omitted, the application uses relative /api endpoints
VITE_API_BASE_URL=
